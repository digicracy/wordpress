# Default before peer relation is handled
server {
  listen 80 default;
  server_name _;
  location / {
    proxy_pass http://127.0.0.1:8080;
  }
}

server {
    listen 80;
    server_name cdn.omgubuntu.co.uk;
    rewrite ^ $scheme://omgubuntu.co.uk$request_uri permanent;
}
