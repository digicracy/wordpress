#!/bin/bash

# Run the install again!
hooks/install

wp_install_path="/var/www/"

juju-log "Upgrading User specific files: Themes, plugins, etc in to position"
rsync -razvP files/wordpress/ $wp_install_path
chown -R www-data.www-data $wp_install_path

source hooks/restart
